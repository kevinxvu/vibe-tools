version: "3"
services:
  db:
    platform: linux/x86_64
    image: mariadb:12
    container_name: vibetools.db
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: vibetools
      MYSQL_USER: vibetools
      MYSQL_PASSWORD: vibetools123
      MYSQL_ROOT_PASSWORD: vibetools123
      TZ: UTC
    volumes:
      - db-data:/var/lib/mysql
    restart: unless-stopped

volumes:
  db-data:
